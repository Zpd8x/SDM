# SDM Plugins API v1

Each plugin lives in its own folder and contains `plugin.json` plus an entry Python file.
Plugins are loaded from the user data `plugins` folder. Failures are isolated and shown in System Center.
