# دليل تثبيت إضافة SDM للمتصفح

يدعم SDM v1.0.0 متصفحي Google Chrome وMicrosoft Edge من خلال إضافة واحدة
بتقنية Manifest V3.

## أولًا: تثبيت Native Host

1. فك ضغط حزمة SDM في مجلد ثابت.
2. شغّل `START_SDM.bat` مرة واحدة وانتظر فتح البرنامج.
3. أغلق SDM.
4. شغّل `INSTALL_BROWSER_INTEGRATION.bat`.
5. انتظر ظهور:

```text
[SUCCESS] Browser integration is ready.
```

يُنشئ المثبت خدمة صغيرة باسم `SDMNativeHost.exe` ويسجّلها للمستخدم الحالي
فقط في Chrome وEdge. لا يحتاج المثبت إلى صلاحيات Administrator.

## ثانيًا: تحميل الإضافة

### Chrome

1. افتح `chrome://extensions`.
2. فعّل `Developer mode`.
3. اضغط `Load unpacked`.
4. اختر مجلد `browser_extension` داخل SDM v1.0.0.

### Edge

1. افتح `edge://extensions`.
2. فعّل `Developer mode`.
3. اضغط `Load unpacked`.
4. اختر مجلد `browser_extension` داخل SDM v1.0.0.

معرّف الإضافة الصحيح:

```text
jbeiimgfgpkhnalnajffnafmfedganbh
```

### الترقية من إصدار قديم

1. شغّل `INSTALL_BROWSER_INTEGRATION.bat` من مجلد SDM v1.0.0.
2. افتح صفحة Extensions واحذف نسخة `SDM Integration` القديمة.
3. اضغط `Load unpacked` واختر `browser_extension` من مجلد 1.0.0.
4. ثبّت الأيقونة من جديد ثم تحقق من ظهور Native Host 1.0.0.

سجل التنزيلات محفوظ داخل `%LOCALAPPDATA%\SDM` ولا يُحذف عند تبديل مجلد
البرنامج أو الإضافة.

## ثالثًا: اختبار الاتصال

1. اضغط أيقونة SDM في شريط المتصفح.
2. يجب أن تظهر عبارة `Connected • Native Host 1.0.0` باللون الأخضر.
3. ألصق رابط ملف مباشر.
4. اختر `4 connections`.
5. اترك `Prefer Start Download` مفعّلًا.
6. اضغط `Send to SDM`.

إذا كان SDM مغلقًا، يجب أن يفتح تلقائيًا ويعرض نافذة
`Download File Info` وحدها من دون إظهار الواجهة الرئيسية. اختر منها:

- `Start Download` للبدء فورًا.
- `Download Later` لإضافة الملف متوقفًا.
- `Cancel` لإلغاء الطلب.

يمكنك تغيير الفئة والاسم ومسار الحفظ وعدد الاتصالات وإضافة وصف. عند تفعيل
`Remember this path` يتذكر SDM المجلد للفئة المختارة.

بعد `Start Download` تظهر نافذة تقدم مستقلة تعرض السرعة والوقت المتبقي
وتقدم كل اتصال. استخدم `Pause` للإيقاف المؤقت و`Resume` للاستكمال، بما
في ذلك بعد فشل مؤقت. استخدم `Open SDM` إذا أردت فتح المدير الكامل.

نافذة معلومات الملف ونافذة التقدم لهما الآن رمز SDM مستقل في شريط مهام
Windows، وتظهر نافذة المعلومات فوق المتصفح مباشرة.

## منع التكرار وSmart Rules

إذا اعترضت الإضافة الرابط نفسه مرة أخرى، يقارن SDM هوية المصدر الثابتة بدل
التوقيع المؤقت. التنزيل النشط أو المكتمل لا يُضاف مرتين. أما التنزيل المتوقف
أو الفاشل فتظهر نافذة معلوماته نفسها ويمكن استكمال أجزائه.

من المدير الرئيسي اضغط `Smart Rules` لإنشاء قواعد تلقائية. رتب القواعد
بالأولوية؛ أول قاعدة مطابقة فقط هي التي تُطبق. يمكن مطابقة النطاق ونوع
الوسائط والامتداد وMIME والفئة والحجم، ثم اختيار المجلد والفئة وعدد الاتصالات
والبدء الآن أو لاحقًا.

القواعد الافتراضية تضبط ChatGPT وGoogle Drive على اتصالين لتقليل 403 و429،
وتصنف الصوت والفيديو الملتقطين إلى Music وVideos. يمكن تعديل هذه القواعد أو
حذفها أو استعادتها من `Reset Defaults`.

يُعامل العدد الذي تختاره أو تحدده القاعدة كحد أعلى. يعرض المدير مثلًا
`Adaptive 2/8` عندما يتعلم أن الخادم يحتاج مؤقتًا إلى طلبين متزامنين فقط.
عند 429 أو 503 ينخفض الحد دون حذف أجزاء الاستكمال، ثم يرتفع تدريجيًا بعد
استقرار الخادم.

## محولات Google Drive وDropbox وOneDrive وملفات ChatGPT

فعّل `Automatically capture browser downloads` ثم أعد تحميل الإضافة. عند
بدء تنزيل من Google Drive أو من رابط ملف داخل ChatGPT، تنتظر الإضافة قليلًا
حتى يحدد المتصفح الاسم والحجم والرابط النهائي. إذا بقي الاسم `download` أو
`content` يفحص Native Host رؤوس الخادم دون تنزيل الملف كاملًا.

يحفظ الإصدار 1.0.0 الرابط الأصلي الثابت أيضًا. قبل النقل يجلب رابطًا مباشرًا
جديدًا من المصدر، ويعيد ذلك تلقائيًا إذا انتهى توقيع الرابط. قد يخفض SDM
عدد الاتصالات لخدمات التخزين لتجنب الحظر المؤقت؛ وهذا سلوك مقصود.

بعض الملفات الخاصة لا تسمح بالوصول خارج جلسة Chrome أو Edge. لهذه الملفات:

1. افتح نافذة إضافة SDM.
2. فعّل `Use browser session for private file downloads`.
3. وافق على صلاحية Cookies التي يطلبها المتصفح.
4. ابدأ تنزيل الملف مرة أخرى.

الخيار معطّل افتراضيًا ويعمل للملفات المباشرة فقط. تجمع الإضافة Cookies
المطابقة للرابط المحدد فقط، ثم يحميها Windows DPAPI للمستخدم الحالي. لا
تُحفظ داخل SQLite أو Logs، وتنتهي بعد 24 ساعة وتُحذف عند اكتمال الملف أو
حذف التنزيل. عند إطفاء الخيار تُزال صلاحية Cookies من الإضافة.

في ChatGPT يقرأ SDM اسم الملف من رابط التنزيل داخل صفحة المحادثة قبل أن
يعيد الخادم الاسم العام `content`. يجب تحديث صفحة ChatGPT بعد Reload للإضافة
حتى يعمل Content Script الجديد.

## اكتشاف الصوت التلقائي

يمكن أن تظهر `Download this audio` دون تمرير المؤشر عندما تكتشف الإضافة:

- عنصر `<audio>` ظاهرًا أو مخفيًا.
- رابط MP3 أو M4A أو AAC أو OGG أو OPUS أو WAV أو FLAC.
- بيانات Open Graph الخاصة بالصوت.
- Media Session نشطة في صفحة صوتية.
- صفحة عامة في SoundCloud أو Bandcamp أو Mixcloud أو Audiomack أو HearThis.
- استجابة شبكة تحمل `Content-Type: audio/*` حتى لو كان رابطها بلا امتداد.

إذا كان الرابط مباشرًا يرسله SDM كما هو. وإذا كان المشغل يستخدم Blob أو
Streaming، يرسل رابط الصفحة إلى محرك الوسائط. DRM والصفحات الخاصة قد لا
تسمح بالاستخراج.

## صلاحية Network Capture والجلسة الاختيارية

تطلب النسخة 0.5.1 صلاحية `webRequest` مع HTTP/HTTPS لتتعرف على نوع استجابة
الصوت أو الفيديو واسمها وحجمها. تفحص الإضافة عنوان الاستجابة ورؤوسها فقط،
ولا تقرأ جسم الملف أو كلمات المرور أو بيانات النماذج. لا تُقرأ Cookies إلا
إذا فعّلت `Secure Session Bridge` بنفسك؛ وعندها تُقرأ Cookies المطابقة لرابط
التنزيل المحدد فقط.

## لوحة Download this video/audio

خيار `Show “Download this video/audio” panel` مفعّل افتراضيًا. مرّر مؤشر
الفأرة فوق فيديو أو صوت HTML5 مباشر لتظهر لوحة SDM الصغيرة:

- اضغط اسم اللوحة لإرسال رابط الوسائط إلى SDM.
- اضغط `?` لمعرفة حدود الدعم.
- اضغط `×` لإخفائها عن هذا المشغّل حتى تحديث الصفحة.
- حدّث صفحات الويب المفتوحة بعد Reload للإضافة.

إذا وجد SDM رابط HTTP/HTTPS مباشرًا يستخدمه فورًا. وإذا كان المشغّل يستخدم
`blob:` أو HLS/DASH، يرسل رابط الصفحة إلى محرك الوسائط ليحلله عند بدء
التنزيل. تعمل هذه الطريقة مع الصفحات العامة في YouTube وInstagram وFacebook
وSoundCloud وTikTok وVimeo وX وDailymotion. لا يدعم SDM محتوى DRM، وقد تفشل
الصفحات الخاصة التي تتطلب تسجيل دخول.

## اختبار قائمة الزر الأيمن

انقر بزر الفأرة الأيمن على رابط مباشر ثم اختر:

```text
Download link with SDM
```

بالنسبة إلى صورة أو صوت أو فيديو مباشر يظهر:

```text
Download media with SDM
```

وعند النقر بزر الفأرة الأيمن على مساحة فارغة في الصفحة يظهر:

```text
Download with SDM
```

استخدمه لإرسال صفحة الفيديو أو الصوت نفسها إلى محرك الوسائط.

## الاعتراض التلقائي

خيار `Automatically capture browser downloads` معطّل افتراضيًا. بعد نجاح
اختبار الاتصال يمكنك تفعيله من نافذة الإضافة. عندها يرسل المتصفح التنزيل
إلى SDM، وبعد تأكيد الاستلام يلغي نسخة التنزيل التي بدأها المتصفح.

لا يعترض SDM روابط `data:` ولا المحتوى المحمي بـDRM. أما صفحات `blob:`
العامة فيمكن إرسال رابط الصفحة منها عبر Media Panel أو قائمة الزر الأيمن.

## حل المشكلات

### Native host has exited أو Native host was not found

- أغلق المتصفح بالكامل.
- شغّل `INSTALL_BROWSER_INTEGRATION.bat` مرة أخرى.
- افتح صفحة Extensions واضغط Reload على إضافة SDM.
- إذا ظهرت رسالة `scriptname` من PyInstaller، فتأكد أنك تستخدم SDM v0.3.1
  أو أحدث؛ هذه النسخة تصلح مشكلة اقتباس مسار المشروع في Windows.

### الإضافة غير متصلة بعد نقل مجلد SDM

المثبت يحفظ المسار الكامل للبرنامج. شغّل
`INSTALL_BROWSER_INTEGRATION.bat` مجددًا بعد نقل المجلد.

### الرابط يظهر Queued

هذا طبيعي عندما يكون عدد الملفات النشطة مساويًا لقيمة `Concurrent`. يبدأ
الملف تلقائيًا عندما ينتهي الملف السابق.

### إزالة الربط

1. شغّل `UNINSTALL_BROWSER_INTEGRATION.bat`.
2. احذف `SDM Integration` من صفحة Extensions.

## الأمان

- الاتصال يتم عبر Native Messaging وليس عبر منفذ شبكة محلي.
- Native Host يقبل رسائل الإضافة ذات المعرّف الثابت فقط.
- تُحقن لوحة الوسائط في صفحات HTTP/HTTPS لاكتشاف عناصر الفيديو والصوت؛ ولا
  تجمع كلمات المرور أو محتوى النماذج ولا ترسل شيئًا حتى تضغط أمر SDM.
- الاعتراض التلقائي لا يعمل حتى تفعّله بنفسك.
- Session Bridge لا يعمل حتى تفعّله بنفسك وتوافق على صلاحية Cookies.
- Cookies لا تظهر في SQLite أو Logs، وتكون مشفرة بواسطة Windows DPAPI.
