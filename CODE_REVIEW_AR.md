# مراجعة كود SDM v1.0.0

تاريخ المراجعة: 2026-07-29

## النتيجة العامة

البنية الحالية منظمة جيدًا ومقسمة إلى محرك تنزيل، قاعدة بيانات، Queue،
محولات مواقع، تكامل متصفح، وواجهة PySide6. جميع اختبارات v0.9.0 الأصلية
نجحت قبل التعديل: 158 اختبارًا و31 اختبارًا فرعيًا. بعد إضافة المرحلة
العاشرة نجح 161 اختبارًا و31 اختبارًا فرعيًا.

## نقاط القوة

- ترحيلات SQLite متوافقة مع الإصدارات القديمة.
- فصل واضح بين محرك النقل والواجهة وقاعدة البيانات.
- دعم جيد للاستكمال، التقسيم، Retry-After، وAdaptive Connections.
- حدود أمان واضحة لجلسات المتصفح وملفات Cookies.
- اختبارات واسعة للعقود البرمجية وتكامل الإضافة.
- عدم حذف الملفات تلقائيًا عند الاشتباه بالتكرار.

## المشكلات التي عولجت في v1.0.0

1. كان SHA-256 يُحسب فقط عند إدخال Checksum متوقع. أصبح يُحسب بعد كل
   تنزيل مكتمل.
2. لم يكن كشف التكرار يعتمد على محتوى الملف نفسه. أضيفت مقارنة محلية حسب
   SHA-256 مستقلة عن الرابط والاسم والمجلد.
3. كان التحقق المتوقع قد يتطلب مسارًا منفصلًا. أصبحت البصمة المحسوبة نفسها
   تُستخدم للتحقق وللكشف عن النسخ.
4. أضيفت حقول SQLite وفهرس للبحث السريع مع ترحيل تلقائي.
5. أضيف تحذير غير تدميري يعرض الملف الجديد والملف المطابق ويُبقيهما معًا.

## ملاحظات تحسين مستقبلية

- ملف `sdm/ui/main_window.py` كبير نسبيًا؛ يفضل لاحقًا تقسيم منطق الجدول،
  العمال، وإدارة الإضافة إلى Controllers مستقلة.
- توجد حدود `except Exception` أخيرة لحماية Threads وNative Host. هي مفيدة
  لمنع الانهيار، لكن يفضل إضافة Logging تفصيلي مع Traceback في إصدار لاحق.
- اختبارات الواجهة الحالية تعتمد جزئيًا على عقود المصدر. يلزم Smoke Test
  فعلي على Windows لـPySide6 وDPAPI وتسجيل Native Messaging.
- محولات المواقع وyt-dlp تحتاج اختبارًا حيًا دوريًا لأن المواقع تتغير.
- للملفات الضخمة جدًا يمكن لاحقًا عرض تقدم مستقل لمرحلة حساب البصمة.

## فحوصات الإصدار

- Python compilation: PASS
- JavaScript syntax: PASS
- Browser manifest JSON: PASS
- SQLite migration and persistence: PASS
- Original regression suite: PASS
- Content fingerprint regression tests: PASS
- Final result: 161 passed, 31 subtests passed
